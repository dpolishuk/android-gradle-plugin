buildscript {
    repositories {
        maven { url '../../repo' }
    }
    dependencies {
        classpath 'com.android.tools.build:gradle-android:0.2-SNAPSHOT'
    }
}

allprojects {
    version = '1.0'

    repositories {
        mavenCentral()
    }
}
